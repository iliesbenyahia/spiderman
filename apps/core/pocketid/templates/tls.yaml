apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pocketid-tls
  namespace: argocd
spec:
  secretName: pocketid-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
  dnsNames:
    - "*.ibenyahia.fr"