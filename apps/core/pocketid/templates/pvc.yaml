apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pocketid-nas-data
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: synology-iscsi-storage
  resources:
    requests:
      storage: 10Gi
