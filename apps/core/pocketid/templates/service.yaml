apiVersion: v1
kind: Service
metadata:
  name: pocketid
  labels:
    app: pocketid
spec:
  type: ClusterIP
  selector:
    app: pocketid
  ports:
    - name: http
      port: 80
      targetPort: http
