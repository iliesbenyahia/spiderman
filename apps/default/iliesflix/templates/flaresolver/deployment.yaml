{{- if .Values.flaresolverr.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flaresolverr
  namespace: {{ .Release.Namespace }}
  labels:
    app: flaresolverr
    app.kubernetes.io/part-of: iliesflix
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flaresolverr
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: flaresolverr
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: role
                    operator: In
                    values:
                      - {{ .Values.flaresolverr.nodeRole | default "iliesflix" | quote }}
      containers:
        - name: flaresolverr
          image: {{ .Values.flaresolverr.image }}
          env:
            - name: LOG_LEVEL
              value: {{ .Values.flaresolverr.logLevel | quote }}
            - name: LOG_HTML
              value: {{ .Values.flaresolverr.logHtml | quote }}
            - name: CAPTCHA_SOLVER
              value: {{ .Values.flaresolverr.captchaSolver | quote }}
            - name: TZ
              value: {{ .Values.global.timezone | quote }}
          ports:
            - containerPort: {{ .Values.flaresolverr.port }}
          livenessProbe:
            tcpSocket:
              port: {{ .Values.flaresolverr.port }}
            initialDelaySeconds: 30
            periodSeconds: 30
          resources: {{- toYaml .Values.flaresolverr.resources | nindent 12 }}
{{- end }}