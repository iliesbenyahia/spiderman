apiVersion: v1
kind: ConfigMap
metadata:
  name: gluetun-scripts
  namespace: {{ .Release.Namespace }}
data:
  port-forward.sh: |
    #!/bin/sh
    wget -q -O- --post-data="json={\"listen_port\":{{PORTS_0}}}" \
      http://localhost:8080/api/v2/app/setPreferences || true