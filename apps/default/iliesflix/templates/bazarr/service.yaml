{{- if .Values.bazarr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: bazarr
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: bazarr
  ports:
    - port: 6767
      targetPort: 6767
{{- end }}
